#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -z "$NODE_PATH" ]; then
  export NODE_PATH="$basedir/D:/Users/david/Documents/Work/Website/GitHub/hnest/node_modules/.store/project-name@0.2.6/node_modules"
else
  export NODE_PATH="$NODE_PATH:$basedir/D:/Users/david/Documents/Work/Website/GitHub/hnest/node_modules/.store/project-name@0.2.6/node_modules"
fi
if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../../../../../project-name@0.2.6/node_modules/project-name/cli.js" "$@"
else
  exec node  "$basedir/../../../../../project-name@0.2.6/node_modules/project-name/cli.js" "$@"
fi
